# dayskew shared types & contracts

Placeholder reserved for OpenAPI / JSON-spec shared types consumed by the
Go API (backend) and future web/mobile clients.

Planned:
- `openapi.yaml` — REST endpoint contract for the backend
- shared JSON-schema definitions mirroring backend `internal/` types